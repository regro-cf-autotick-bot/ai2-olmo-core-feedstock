{% set name = "ai2-olmo-core" %}
{% set version = "2.6.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://files.pythonhosted.org/packages/source/{{ name[0] }}/{{ name }}/ai2_olmo_core-{{ version }}.tar.gz
  sha256: 6b20e4e5a24747c2afb145aad1cd38238429abded6b879819591e50121000c6e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - wheel
    - pip
  run:
    - dataclass-extensions >=0.3.0
    - filelock >=3.4,<4.0
    - pandas
    - pyyaml
    - bettermap
    - python >={{ python_min }}
    - numpy
    - pytorch >=2.6.0
    - cached_path >=1.7.2
    - requests
    - packaging
    - rich
    - omegaconf
    - safetensors
    - importlib_resources
    - ai2-olmo-eval ==0.7.1

test:
  imports:
    - olmo_core
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/allenai/OLMo-core
  summary: Core training module for the Open Language Model (OLMo)
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - xhochy
